node_modules
dist
.next
apps/api/src/generated/
.env
.env.*
!.env.example
*.log
uploads/
.DS_Store
*.tsbuildinfo

# AI agent tooling config (local-only, not part of the app)
.claude/
.agents/

# 로컬 설계 메모 — 커밋하지 않는다
docs/plans/
